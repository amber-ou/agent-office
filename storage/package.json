{
  "name": "@agent-office/storage",
  "private": true,
  "description": "Storage adapters implementing the Agent Office domain ports.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^5.0.1"
  },
  "dependencies": {
    "node-sqlite3-wasm": "^0.8.60"
  }
}
